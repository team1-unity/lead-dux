# Copy this file to .env and fill in real values — .env itself is
# gitignored, this template is the only part that's tracked.

# MapTiler API key (map tiles/styles — MapLibre GL JS map rendering, see
# frontend/template/mapStyle.js) — restrict it to your domain(s) in the
# MapTiler Cloud dashboard once you have a real domain. Free account:
# https://cloud.maptiler.com/account/keys/
VITE_MAPTILER_KEY=

# Google Maps JavaScript API + Places API key (browser key, restricted by
# HTTP referrer to your domain(s) in Google Cloud Console) — see
# frontend/template/googleMaps.js for what it's used for. Still needed for
# now — only the map rendering has migrated off Google so far, Places
# Autocomplete (address search fields) hasn't yet.
VITE_GOOGLE_MAPS_API_KEY=
