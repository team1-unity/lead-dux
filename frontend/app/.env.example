# Copy this file to .env and fill in real values — .env itself is
# gitignored, this template is the only part that's tracked.

# Google Maps JavaScript API + Places API key (browser key, restricted by
# HTTP referrer to your domain(s) in Google Cloud Console) — see
# frontend/template/googleMaps.js for what it's used for.
VITE_GOOGLE_MAPS_API_KEY=
